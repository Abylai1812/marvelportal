import spinner from "./spinner.gif";

const Spinner = () => {
    return (
        <img style={{margin: '0 auto', background: 'none', display: 'block'}} width="64px" height="64px" src={spinner} alt="spinner"/>
    )
}

export default Spinner;